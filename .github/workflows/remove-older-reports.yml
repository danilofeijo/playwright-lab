name: Delete Old Reports

on:
  # schedule:
  #   - cron: '0 12 * * 5' # At 12:00 UTC, only on Friday
  workflow_run:
    workflows: ['Playwright Tests']
    branches: main
    types:
      - completed
  workflow_dispatch:
    inputs:
      n_days:
        description: 'Reports Age (E.g. 10 will delete reports older than 10 days)'
        required: true
        type: number
        default: 15
      delete_dev:
        description: 'Clean up Dev reports (true by default).'
        required: true
        type: boolean
        default: true
      delete_stg:
        description: 'Clean up Stage reports (true by default).'
        required: true
        type: boolean
        default: true

jobs:
  delete_old_folders:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      N_DAYS: ${{ github.event.inputs.n_days || '15' }} # Defaults to 15 days
      DELETE_DEV: ${{ github.event.inputs.delete_dev != 'false' }} # Defaults to true
      DELETE_STG: ${{ github.event.inputs.delete_stg != 'false' }} # Defaults to true

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: gh-pages

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.9

      - name: Run the script (dev-results)
        if: ${{ env.DELETE_DEV == 'true' }}
        run: python rm_old_folders.py --n-days ${{ env.N_DAYS }} --folder-name ./dev-results

      - name: Run the script (stg-results)
        if: ${{ env.DELETE_STG == 'true' }}
        run: python rm_old_folders.py --n-days ${{ env.N_DAYS }} --folder-name ./stg-results

      - name: Generate manifests (dev-results, stg-results)
        run: |
          set -e

          generate_manifest () {
            env_dir="$1"
            mkdir -p "$env_dir"

            runs=$(ls -1 "$env_dir" 2>/dev/null | grep -E '^[0-9]{8}_[0-9]{6}Z$' | sort -r || true)

            echo '{ "runs": [' > "$env_dir/manifest.json"
            first=1
            for folder in $runs
            do
              if [ $first -eq 0 ]; then echo ',' >> "$env_dir/manifest.json"; fi
              first=0
              echo "  { \"folder\": \"${folder}\", \"url\": \"${env_dir}/${folder}/index.html\" }" >> "$env_dir/manifest.json"
            done
            echo '] }' >> "$env_dir/manifest.json"
          }

          generate_manifest "dev-results"
          generate_manifest "stg-results"

      - name: Commit all changed files back to the repository
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          branch: gh-pages
          commit_message: Delete folders older than ${{ github.event.inputs.n_days }} days
